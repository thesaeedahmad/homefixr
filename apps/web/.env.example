# HomeFixr Web — environment variables (PLACEHOLDERS ONLY)
# Copy this file to ".env.local" for local development.

# Base URL of the HomeFixr API. NEXT_PUBLIC_ vars are exposed to the browser.
NEXT_PUBLIC_API_URL=http://localhost:4000/api
