# Production environment for the web app (read by `next build`).
# This is the PUBLIC API base URL (not a secret) — safe to commit.
# A Vercel dashboard variable of the same name, if set, overrides this.
NEXT_PUBLIC_API_URL=https://homefixr-api.onrender.com/api
